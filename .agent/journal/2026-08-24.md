# 2026-08-24

- Split out of `oddbeaker-framework` monorepo (`packages/oddbeaker-tts/`) into standalone git root at `../dev/oddbeaker-tts`.
- License set to **Apache-2.0**; README rewritten for general-purpose open-source use.
- `pyproject.toml` description/license/classifiers updated; package name remains `oddbeaker-tts`.
- Live ai-trainer user unit retargeted to new path; `:9201` health OK after restart.
- Tests: 18 passed (`pytest -q` with package `.venv`).
- GitHub remote not set — waiting on operator URL.
- Set origin `ssh://git@github.com/OddbeakerLLC/oddbeaker-tts.git`; pushed `master`.
- Merged `master` into `main` (unrelated histories; kept Apache-2.0 LICENSE) and pushed; remote HEAD is `main` @ `8c0a56f` with full tree (`src/`, `tests/`, README, pyproject, etc.).
